name: Windows Release

on:
  push:
    tags:
      - "windows-v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.1
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Fetch pinned local models
        run: node scripts/fetch-models.mjs windows

      - name: Build Windows with bundled models
        run: pnpm --filter @photo-cake/windows tauri build --config src-tauri/tauri.release.conf.json

      - name: Upload Windows bundles
        uses: actions/upload-artifact@v4
        with:
          name: photo-cake-windows
          path: target/release/bundle/**

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: target/release/bundle/**
